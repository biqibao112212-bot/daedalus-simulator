{
  "schema_version": 1,
  "product": "daedalus-simulator",
  "architecture": {
    "family": "x86",
    "name": "x86_64",
    "rust_targets": ["x86_64-unknown-linux-gnu"],
    "arm_targets": "not supported by this release",
    "i686_targets": "not supported by this release"
  },
  "gpu_and_inference_boundary": {
    "simulator_bundles_inference_runtime": false,
    "simulator_links_cuda_or_tensorrt": false,
    "inference_owner": "consumer",
    "render_adapter_selection": "wgpu high-performance adapter within the selected OS backend",
    "runtime_capabilities_file": "daedalus-runtime-capabilities-v1.json",
    "version_selection": "consumer-build-profile",
    "compatibility_rule": "Build the consumer bridge against the selected CUDA/TensorRT profile; the simulator package is unchanged.",
    "validated_profiles": []
  },
  "targets": [
    {
      "id": "linux-x86_64",
      "os": "linux",
      "rust_target": "x86_64-unknown-linux-gnu",
      "binary": "daedalus",
      "render_backends": {
        "performance_default": "vulkan",
        "visible_default": "vulkan",
        "diagnostic_options": ["vulkan"]
      },
      "release_acceptance_scope": "native x86_64 build/SDK tests plus software Vulkan startup and IPC diagnostics; discrete-GPU performance is not a release gate",
      "driver_requirements": [
        "A system Vulkan loader with either a hardware driver or a software implementation such as llvmpipe",
        "A hardware Vulkan GPU is recommended for real-time RGB capture but is not required by the internal-lab Linux acceptance gate",
        "Do not bundle GPU drivers with the release"
      ]
    }
  ]
}
